# PostgreSQL connection (adjust user, password, host, port, database name)
# Matches docker-compose `db` service when using: npm run docker:db
DATABASE_URL="postgresql://procyon:procyon@localhost:5432/procyon?schema=public"

# Archived import files (absolute path or relative to project root). Created automatically on first import.
REPORTS_DIR="./data/reports"
